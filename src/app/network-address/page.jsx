import React from 'react';
import NetworkAddress from '../components/network-address/network-address';

export default function NetworkAddressPage() {
    return (
        <div>
           <NetworkAddress />
        </div>
    );
}